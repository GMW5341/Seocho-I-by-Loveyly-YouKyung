# 서초아이미술 통합 운영 시스템 - 기능 명세서

> **버전:** 1.1.0
> **최종 수정일:** 2026-02-20
> **기술 스택:** React 19 + TypeScript + Vite + Tailwind CSS + Firebase

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [기술 스택](#2-기술-스택)
3. [데이터 모델](#3-데이터-모델)
4. [화면 구성 (11개 탭)](#4-화면-구성)
5. [핵심 아키텍처 결정](#5-핵심-아키텍처-결정)
6. [서비스 & 유틸리티](#6-서비스--유틸리티)
7. [사용자 워크플로우](#7-사용자-워크플로우)
8. [파일 구조](#8-파일-구조)

---

## 1. 프로젝트 개요

미술 학원 운영에 필요한 전반적인 관리를 하나의 웹 애플리케이션에서 처리하는 SPA(Single Page Application).

- 원생 관리, 결제, 스케줄, 출결, 매출 분석 등을 통합 관리
- 로컬 스토리지 기반 데이터 저장 + Firebase 클라우드 동기화 지원
- 모바일 반응형 디자인

---

## 2. 기술 스택

| 구분 | 기술 | 버전 |
|------|------|------|
| UI 프레임워크 | React | 19.2.0 |
| 언어 | TypeScript | - |
| 빌드 도구 | Vite | 7.3.1 |
| 스타일링 | Tailwind CSS | 4.1.18 |
| 클라우드 동기화 | Firebase | 12.9.0 |
| 날짜 처리 | date-fns | 4.1.0 |
| 차트 | Recharts | 3.7.0 |
| 엑셀 내보내기 | xlsx | 0.18.5 |
| ID 생성 | uuid | 13.0.0 |

---

## 3. 데이터 모델

### 3.1 기본 타입

| 타입명 | 값 |
|--------|-----|
| DayOfWeek | '월' \| '화' \| '수' \| '목' \| '금' \| '토' |
| ClassDuration | 60 \| 80 \| 100 (분) |
| ClassLevel | '유아반' \| '초등(저학년)' \| '초등(고학년)' |
| StudentGrade | '6세' \| '7세' \| '초등1' ~ '초등6' |
| PaymentMethod | '계좌이체' \| '현금' \| '카드' \| '온누리상품권' \| '기타' |
| AttendanceStatus | '출석' \| '결석' \| '보강' \| '예정' |
| SeasonType | '학기중' \| '방학중' |
| Gender | '남' \| '여' |

### 3.2 주요 모델

#### Student (원생)

| 필드 | 타입 | 설명 |
|------|------|------|
| id | string | 고유 ID |
| name | string | 이름 |
| gender | Gender | 성별 |
| grade | StudentGrade | 학년 |
| level | ClassLevel | 반 |
| classDuration | ClassDuration | 수업 시간 |
| sessionsPerWeek | number | 주당 횟수 |
| regularSchedule | {day, startTime}[] | 정규 스케줄 (결제에서 동기화됨) |
| phone | string | 학생 연락처 |
| parentPhone | string | 학부모 연락처 |
| memo | string | 메모 |
| active | boolean | 재원 여부 |
| createdAt | string | 등록일 |

#### Payment (결제)

| 필드 | 타입 | 설명 |
|------|------|------|
| id | string | 고유 ID |
| studentId | string | 원생 ID |
| amount | number | 최종 결제 금액 |
| originalAmount | number | 원래 금액 |
| discountRate | number | 할인율 (%) |
| extraDiscounts | {name, rate?, amount?}[] | 추가 할인 항목 |
| totalSessions | number | 총 수업 횟수 |
| usedSessions | number | 사용한 횟수 |
| remainingSessions | number | 잔여 횟수 |
| completed | boolean | 소진 완료 여부 |
| method | PaymentMethod | 결제 수단 |
| splitPayments | {method, amount}[] | 분할 결제 |
| startDate | string | 시작일 |
| paidAt | string | 결제일 |
| lastClassDate | string | 마지막 수업일 |
| classDuration | ClassDuration | 수업 시간 |
| sessionsPerWeek | number | 주당 횟수 |
| regularSchedule | {day, startTime}[] | **정규 스케줄 (핵심 데이터)** |
| memo | string | 메모 |

#### ScheduleSlot (스케줄 슬롯)

| 필드 | 타입 | 설명 |
|------|------|------|
| id | string | 고유 ID |
| studentId | string | 원생 ID |
| dayOfWeek | DayOfWeek | 요일 |
| startTime | string | 시작 시간 (HH:mm) |
| duration | ClassDuration | 수업 시간 |
| isRegular | boolean | 정규 여부 |
| isTrial | boolean | 체험 수업 여부 |
| trialStudentId | string | 체험 원생 ID |
| date | string | 특정 날짜 (보강 등) |
| isOverrideHidden | boolean | 특정 주 숨김 마커 |

> **참고:** 정규 스케줄(isRegular=true)은 더 이상 store에 저장되지 않으며, 활성 결제(Payment) 데이터에서 실시간으로 파생됩니다.

#### AttendanceRecord (출결)

| 필드 | 타입 | 설명 |
|------|------|------|
| id | string | 고유 ID |
| studentId | string | 원생 ID |
| date | string | 날짜 |
| status | AttendanceStatus | 출결 상태 |
| startTime | string | 시작 시간 |
| duration | ClassDuration | 수업 시간 |
| scheduleSlotId | string | 스케줄 슬롯 ID |
| isMakeup | boolean | 보강 여부 |
| memo | string | 메모 |

#### TrialStudent (체험 원생) & TrialLesson (체험 수업)

| 필드 | 설명 |
|------|------|
| TrialStudent | 이름, 학년, 학부모 연락처, 메모 |
| TrialLesson | 체험 원생 ID, 날짜, 시간, 시간(분), 결제 여부 |

- **체험 수업 가격표:** 60분: ₩35,000 / 80분: ₩40,000 / 100분: ₩45,000

#### SpecialClass (특강) & SpecialClassStudent (특강 수강생)

| 필드 | 설명 |
|------|------|
| SpecialClass | 수업명, 시작일~종료일, 시간(분), 정원, 수강료, 스케줄, 메모 |
| SpecialClassStudent | 이름, 학년, 연락처, 추가 비용 항목, 결제 여부 |

#### 기타 모델

- **CurriculumFile:** 커리큘럼 이미지/PDF (IndexedDB 저장)
- **MessageTemplate:** 메시지 양식 (제목, 내용, 카테고리)
- **Holiday:** 공휴일/휴원일 (날짜 또는 범위)
- **AcademySettings:** 운영 설정 (시즌, 정원, 요금, 운영시간 등)

---

## 4. 화면 구성

### 4.1 커리큘럼 (curriculum)

반별(유아반, 초등 저/고학년) 커리큘럼 자료를 관리합니다.

- 이미지/PDF 업로드 (드래그 앤 드롭 지원)
- 업로드 시 이미지 자동 압축 (최대 800px, 50% 품질)
- 최신 커리큘럼을 크게 표시, 이전 자료는 사이드바에 축소 표시
- 드래그로 순서 변경 (현재 ↔ 과거)
- 클릭 시 전체 화면 확대 보기
- **IndexedDB에 이미지 저장** (localStorage 용량 한계 회피)

### 4.2 대시보드 (dashboard)

학원 운영 현황을 한눈에 파악합니다.

**주요 지표 카드 (7개):**
- 총 원생 수
- 오늘의 수업 수 (공휴일 반영)
- 이번 달 매출
- 결제 건수
- 미결제 원생 수
- 결제 만료 임박 (잔여 1회 이하)
- 보강 필요 원생 수

**차트:**
- 월별 매출 추이 (막대 그래프, 최근 6개월)
- 주간 출결 현황 (누적 막대 그래프)
- 원생 반별 분포 (원형 그래프)

**리스트:**
- 보강 필요 원생 (미처리 건수 순 정렬)
- 오늘의 스케줄 (미결제/공휴일 표시)
- 결제 만료 임박 알림

**오늘의 명언:** 42개 명언 중 날짜 기반 순환 (공휴일/주말 미표시)

### 4.3 스케줄 관리 (schedule)

주간 시간표를 시각적으로 관리합니다.

**핵심 기능:**
- 주 단위 탐색 (이전/다음 주)
- 요일 클릭 시 하루 상세 보기
- 검색으로 특정 원생 필터링

**스케줄 파생 방식:**
- **정규 스케줄:** 활성 결제(Payment)의 regularSchedule에서 실시간 파생
- **보강/임시:** schedules 스토어에 직접 저장
- **체험 수업:** 체험 수업 등록 시 자동 생성
- **특강:** 특강 데이터에서 가상 슬롯 생성

**색상 코드:**

| 유형 | 색상 |
|------|------|
| 60분 | 시안(Cyan) |
| 80분 | 핑크(Pink) |
| 100분 | 노랑(Yellow) |
| 보강 | 주황 점선 테두리 |
| 체험 | 에메랄드 그린 |
| 특강 | 로즈 |
| 미결제 | 빨강 점선 테두리 + "미결제" 라벨 |
| 출석 | 초록 |
| 결석 | 빨강 |
| 보강 출석 | 파랑 |

**드래그 앤 드롭:**
- 시간표 블록을 다른 요일/시간으로 이동
- 10분 단위 스냅
- 운영 시간 및 정원 초과 검증
- 정규 슬롯 이동 시 해당 주에만 임시 적용

**슬롯별 출결:**
- 호버 시 출/결/보 퀵 버튼
- 하루에 여러 수업이 있는 원생도 각각 개별 출결 처리
- 메모 기능 (노란색 하이라이트 표시)

**삭제 & 되돌리기:** 5초 내 실행 취소 가능

### 4.4 원생 관리 (students)

원생 정보를 등록·수정·관리합니다.

- 테이블 형태 목록 (이름, 성별, 학년, 반, 연락처)
- 반별 필터링
- 이름 검색
- 퇴원 원생 표시/숨기기 토글
- 학년순 → 이름순 정렬

**원생 등록/수정:**
- 이름, 성별, 학년, 반, 연락처(학생/학부모), 메모
- 스케줄은 결제(Payment)에서 관리

**원생 액션:**
- 수정, 퇴원(비활성화), 영구 삭제(연관 데이터 전체 삭제)

### 4.5 출결 관리 (attendance)

주간 출결을 그리드 형태로 관리합니다.

**그리드 구성:**
- 행: 원생 (반, 수업시간, 잔여 횟수 표시)
- 열: 화~토 요일
- 셀: 해당 요일 스케줄 시간별 출/결/보 버튼

**시간별 개별 출결:** 같은 날 여러 수업이 있으면 시간 라벨과 함께 각각 표시

**주간 요약:** 원생별 출석/결석 횟수 (예: "3출 1결")

**통계 카드:**
- 주간 출석률
- 보강 필요 원생 수
- 잔여 1회 이하 원생 수

**원생 필터:** 검색 또는 특정 원생 선택

### 4.6 결제 관리 (payments)

결제 등록, 추적, 이력 관리를 수행합니다.

**결제 등록 폼:**

1. **원생 선택** (검색 지원)
2. **수업 시간:** 60 / 80 / 100분
3. **수업 횟수:** 퀵 버튼(3,4,5,8,12,16) + 직접 입력
4. **스케줄 설정:**
   - 주당 횟수 (1~5회)
   - 각 회차: 요일 + 시간 선택
   - 결제와 함께 저장 → 원생 데이터에 동기화
5. **금액 계산:**
   - 자동: (기본 요금 / 4) × 횟수
   - 수동 입력 가능
   - 할인율 (%)
   - 추가 할인 (여러 항목, 비율 또는 정액)
   - 최종 금액 자동 계산
6. **결제 수단:**
   - 단일 수단 선택
   - 분할 결제 모드 (여러 수단 + 금액)
7. **날짜:** 결제일, 시작일 / 마지막 수업일(과거 기록 모드)
8. **과거 기록 모드:** 즉시 소진 완료로 등록

**결제 테이블:**
- 활성 결제: 잔여 횟수, 예상 마지막 수업일, D-day
- 필터: 활성 / 미결제 / 전체
- 이름 검색

**결제 이력:**
- 전체 결제 목록 (활성 + 완료)
- 소진 진행률 바
- 결제 수단 표시 (분할 결제 포함)
- 할인 표시

**자동 횟수 차감:**
- 출석/보강 → 잔여 횟수 자동 차감
- 상태 변경(출석→결석 등) → 자동 보정
- 출결 삭제 → 횟수 복원
- 전체 소진 시 completed 자동 처리

**엑셀 내보내기:** 전체 결제 이력

### 4.7 체험 수업 (trial)

체험 수업 원생과 수업을 관리합니다.

- 체험 원생 등록 (이름, 학년, 학부모 연락처)
- 수업 등록 (날짜, 시간, 시간(분))
- 가격표 자동 표시
- 결제 처리 (결제 수단, 결제일)
- 스케줄 그리드에 자동 반영 (에메랄드 그린)

**통계:** 총 체험 원생, 총 수업 수, 미결제 건수, 매출

### 4.8 메시지 양식 (messages)

카카오톡 등에 사용할 메시지 양식을 관리합니다.

- 양식 등록 (제목, 내용, 카테고리)
- **카테고리:** 수업 안내, 결제 안내, 휴원 안내, 보강 안내, 체험 안내, 기타
- 카드 형태 목록
- 클립보드 복사 버튼
- 카테고리별 필터 (건수 표시)
- 제목/내용 검색

### 4.9 특강 수업 (special)

방학 중 특강 프로그램을 관리합니다.

- **방학중 모드에서만 이용 가능** (설정에서 전환)

**특강 개설:**
- 수업명, 시작~종료일, 시간(분), 정원, 수강료
- 여러 스케줄 항목 (요일 + 시간)
- 메모

**수강생 관리:**
- 이름, 학년, 연락처
- 추가 비용 항목 (재료비 등)
- 총 금액 = 기본 수강료 + 추가 비용
- 개별 결제 처리

**통계:** 등록 인원, 결제 완료, 미결제, 매출

**스케줄 연동:** 로즈색 블록으로 스케줄 그리드에 표시 (이동 불가)

### 4.10 매출 현황 (revenue)

수입을 분석하고 리포트를 생성합니다.

**요약 카드:** 월 매출, 연 매출, 특강 누적 매출

**매출 분석:**
- 카테고리별 (정규, 체험, 특강) - 월별/연별
- 결제 수단별 - 월별/연별 (비율 바)
- 수업 시간별 - 월별

**월별 추이 차트:**
- 기간 설정: 3 / 6 / 12개월
- 누적 막대 그래프 (정규 + 체험 + 특강)

**엑셀 내보내기 (4개 시트):**
1. 월별 상세 (모든 수입원)
2. 연별 상세
3. 월별 카테고리 요약
4. 결제 수단별 요약

### 4.11 설정 (settings)

시스템 환경을 설정합니다.

1. **로고 업로드:** 이미지 → 정사각형 크롭 → 200×200px 리사이즈
2. **시즌 모드:** 학기중 ↔ 방학중 전환
3. **정원:** 시간당 최대 원생 수 (1~20명)
4. **요금표:** 60분/80분/100분 기본 요금 (4회 기준, 회당 금액 자동 계산)
5. **운영 시간:** 학기중/방학중 별도 설정, 요일별 시작~종료 시간
6. **공휴일/휴원일:** 단일 날짜 또는 범위, 이름 지정
7. **클라우드 동기화:**
   - Firebase 설정 (코드 붙여넣기)
   - 동기화 방 이름
   - 수동 업로드
   - 모바일 링크 생성 (자동 설정 URL)
   - JSON → 클라우드 업로드
   - 동기화 해제
8. **데이터 관리:**
   - 백업 (JSON 내보내기)
   - 복원 (JSON 가져오기)
   - 전체 초기화

---

## 5. 핵심 아키텍처 결정

### 5.1 결제 기반 스케줄 시스템

```
[이전] Payment → ScheduleSlot(isRegular) → store → ScheduleGrid
[현재] Payment.regularSchedule → 실시간 파생 → ScheduleGrid
```

- 정규 스케줄은 **활성 결제(Payment)**의 `regularSchedule`에서 실시간 파생
- 스케줄 슬롯을 별도 저장하지 않음 → 데이터 이중 관리 문제 해소
- **결제 소진 시** 스케줄 자동 소멸
- **미결제 폴백:** 활성 결제 없으면 마지막 완료 결제의 스케줄을 빨간 점선("미결제")으로 표시

### 5.2 슬롯 오버라이드 시스템

- 정규 슬롯은 결제에서 파생되므로 store에 존재하지 않음
- 특정 주에 숨기려면: `isOverrideHidden` 마커를 store에 추가
- 특정 주에 이동하려면: 숨김 마커 + 새 임시 슬롯 추가
- 복원 시: 마커 제거 → 정규 슬롯이 다시 나타남

### 5.3 하루 다중 스케줄

- 한 원생이 같은 날 여러 시간에 수업 가능
- `regularSchedule: [{day, startTime}, ...]` 배열로 관리
- 출결은 `startTime`으로 각 슬롯 구분

### 5.4 출결-결제 자동 연동

- 출석/보강 기록 → `remainingSessions` 자동 차감
- 상태 변경 (출석 → 결석) → 횟수 자동 보정
- 출결 삭제 → 횟수 복원
- 전체 소진 시 `completed = true` 자동 처리

### 5.5 IndexedDB 이미지 저장

- 커리큘럼 이미지를 IndexedDB에 저장
- 메타데이터만 localStorage에 보관
- localStorage 5MB 용량 한계 회피
- 기존 dataUrl → IndexedDB 마이그레이션 로직 포함

### 5.6 클라우드 동기화

- `CloudDataLoader`가 React 렌더 전에 클라우드 데이터 선행 로드
- 빈 로컬 데이터로 클라우드 덮어쓰기 방지
- 실시간 리스너로 원격 변경 감지 → 자동 페이지 리로드
- 1초 디바운스 Push로 API 호출 절감
- 타임스탬프 + 플래그로 동기화 루프 방지
- 대용량 데이터 자동 청크 분할 (800KB 단위)

---

## 6. 서비스 & 유틸리티

### 6.1 Firebase 동기화 (`src/services/firebaseSync.ts`)

| 함수 | 설명 |
|------|------|
| setupSync() | 동기화 초기화 (config + room) |
| pushToCloud() | 로컬 → 클라우드 업로드 |
| fetchCloudData() | 클라우드 → 로컬 다운로드 (1회) |
| startRealtimeSync() | 원격 변경 실시간 감지 |
| stopSync() | 리스너 해제 |
| uploadJsonToCloud() | JSON 직접 업로드 |
| generateSyncUrl() | 공유 URL 생성 |
| checkUrlForSyncConfig() | URL 해시 파싱 |

### 6.2 커리큘럼 이미지 저장소 (`src/services/curriculumImageStore.ts`)

| 함수 | 설명 |
|------|------|
| saveCurriculumImage() | 이미지 저장 |
| getCurriculumImage() | 이미지 조회 |
| deleteCurriculumImage() | 이미지 삭제 |
| getAllCurriculumImages() | 전체 조회 |

### 6.3 유틸리티 (`src/utils/helpers.ts`)

**날짜/시간:**
- `formatDate()`, `formatShortDate()` - 한국어 날짜 포맷
- `getDayOfWeekFromDate()` - 날짜 → 요일 변환
- `generateTimeSlots()` - 시간 간격 생성
- `getEndTime()` - 종료 시간 계산
- `isTimeOverlapping()` - 시간 중복 확인

**금액:**
- `formatCurrency()` - 원화 포맷
- `getPricePerSession()` - 횟수별 회당 가격 계산

**결제 계산:**
- `calculateNextPaymentDate()` - 다음 결제 예상일
- `calculateLastClassDate()` - 마지막 수업 예상일 (출결, 공휴일 반영)

**스케줄 레이아웃:**
- `layoutSlotsForDay()` - 겹치는 슬롯 다단 배치 알고리즘

### 6.4 엑셀 내보내기 (`src/utils/excelExport.ts`)

| 함수 | 설명 |
|------|------|
| exportPaymentsToExcel() | 결제 이력 엑셀 (단일 시트) |
| exportRevenueToExcel() | 매출 현황 엑셀 (4개 시트) |

---

## 7. 사용자 워크플로우

### 7.1 신규 원생 등록 → 첫 결제

1. **원생 관리** → 원생 추가 (이름, 학년, 반)
2. **결제 관리** → 결제 등록
3. 원생 선택, 시간(분), 횟수 설정
4. 스케줄 설정 (주당 횟수 + 요일/시간)
5. 금액 설정 (할인 적용)
6. 결제 수단 선택
7. 등록 → 스케줄 자동 반영

### 7.2 일일 출결 처리

1. **스케줄 관리** → 이번 주 보기
2. 스케줄 블록 호버 → 출/결/보 클릭
3. 필요 시 메모 추가
4. 결제 잔여 횟수 자동 차감

### 7.3 보강 수업 등록

1. **스케줄 관리** → "+ 보강 추가"
2. 원생, 요일, 시간 선택
3. "자동 출결" 체크 시 보강 출석 즉시 처리
4. 주황 점선 블록으로 표시

### 7.4 체험 수업 등록

1. **체험 수업** → "+ 체험 수업 등록"
2. 원생 정보 입력 (이름, 학년, 연락처)
3. 날짜, 시간, 시간(분) 선택
4. 등록 → 스케줄에 에메랄드 그린 블록 표시
5. 수업 후: 결제 처리

### 7.5 특강 운영 (방학)

1. **설정** → 방학중 모드 전환
2. **특강 수업** → "+ 특강 개설"
3. 수업 정보 입력 (이름, 기간, 스케줄, 수강료)
4. 수강생 추가 (추가 비용 항목 설정 가능)
5. 개별 결제 처리
6. 스케줄 그리드에 로즈색 블록 표시

### 7.6 월말 매출 확인

1. **매출 현황** → 월 선택
2. 카테고리별/결제 수단별 분석 확인
3. "엑셀 다운로드" → 상세 리포트

### 7.7 클라우드 동기화 설정

1. **설정** → 클라우드 동기화
2. Firebase 설정 코드 붙여넣기
3. 방 이름 입력
4. "동기화 시작" 클릭
5. 모바일: 공유 링크 열기 → 자동 설정

### 7.8 데이터 백업 & 복원

1. **설정** → 데이터 관리
2. "데이터 백업" → JSON 파일 다운로드
3. 다른 기기: "데이터 복원" → JSON 파일 선택 → 자동 리로드

---

## 8. 파일 구조

```
src/
├── App.tsx                              # 메인 앱 (라우팅, 클라우드 로더)
├── main.tsx                             # React 루트
├── types/
│   └── index.ts                         # 전체 TypeScript 타입 정의
├── store/
│   ├── useStore.ts                      # 상태 관리 (전체 CRUD)
│   └── StoreContext.tsx                 # Context Provider
├── services/
│   ├── firebaseSync.ts                  # 클라우드 동기화
│   └── curriculumImageStore.ts          # IndexedDB 이미지 저장
├── utils/
│   ├── helpers.ts                       # 날짜, 금액, 레이아웃 유틸
│   └── excelExport.ts                   # 엑셀 생성
├── components/
│   ├── layout/
│   │   ├── Sidebar.tsx                  # 네비게이션
│   │   └── Header.tsx                   # 상단 바 (시계)
│   ├── common/
│   │   ├── Badge.tsx                    # 상태 배지
│   │   └── Modal.tsx                    # 모달
│   ├── curriculum/
│   │   └── CurriculumPage.tsx           # 커리큘럼 관리
│   ├── dashboard/
│   │   └── Dashboard.tsx                # 대시보드 (차트, 통계)
│   ├── schedule/
│   │   ├── ScheduleGrid.tsx             # 주간 시간표
│   │   ├── MakeupForm.tsx               # 보강 등록 폼
│   │   └── TrialForm.tsx                # 체험 수업 폼
│   ├── students/
│   │   ├── StudentList.tsx              # 원생 목록
│   │   └── StudentForm.tsx              # 원생 등록/수정 폼
│   ├── attendance/
│   │   └── AttendanceManager.tsx        # 출결 관리
│   ├── payments/
│   │   ├── PaymentManager.tsx           # 결제 관리
│   │   └── PaymentForm.tsx              # 결제 등록 폼
│   ├── trial/
│   │   └── TrialManager.tsx             # 체험 수업 관리
│   ├── messages/
│   │   └── MessageTemplates.tsx         # 메시지 양식
│   ├── special/
│   │   └── SpecialClassManager.tsx      # 특강 관리
│   ├── revenue/
│   │   └── RevenueOverview.tsx          # 매출 현황
│   └── settings/
│       └── SettingsPage.tsx             # 설정
```

---

## 변경 이력

| 날짜 | 버전 | 변경 내용 |
|------|------|-----------|
| 2026-02-20 | 1.1.0 | 결제 기반 스케줄 시스템 전환, 미결제 폴백 기능 추가 |
| 2026-02-20 | - | 기능 명세서 최초 작성 |
